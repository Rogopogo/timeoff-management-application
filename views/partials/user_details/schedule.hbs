
<div class="col-md-9">

  <form method="POST" action="/settings/schedule" id="company_schedule_form">
  <input type="hidden" name="user_id" value="{{employee.id}}">

  {{> user_details/breadcrumb employee=employee }}

  <div class="form-group">
    <label for="" class="control-label">График</label>

    <p class="help-block">
      {{# with schedule }}
      {{# if (is_user_specific) }}
        Нынешний сотрудник имеет <strong data-vpp="declare-user-specific-schedule">собственное</strong> расписание.
      {{else}}
        Текущий сотрудник использует <strong><a data-vpp="link-to-company-schedule" href="/settings/general/">общекорпоративный</a></strong> график.
      {{/if}}
      {{/with}}
    </p>

    <div class="input-group">
      {{> schedule_widget }}
    </div>

  </div>
  <div class="form-group">
    <div class="pull-right">
    {{# with schedule }}
    {{# if (is_user_specific) }}
      <button type="submit" name="revoke_user_specific_schedule" class="btn btn-default">Перевести сотрудника на общекорпоративный график</button>
      <button type="submit" name="save_user_specific_schedule" class="btn btn-success">Сохранение расписания для конкретного сотрудника</button>
    {{else}}
      <button type="submit" name="save_user_specific_schedule" class="btn btn-success">Переопределить общекорпоративный график</button>
    {{/if}}
    {{/with}}
    </div>
  </div>
  </form>

</div>
